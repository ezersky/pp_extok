{
  "name": "Tokens to SCSS",
  "description": "The plugin exports all token sets (Global/*, Theme/*, and any other available Sets) to SCSS files, preserving the links between tokens instead of resolved values.",
  "version": 2,
  "code": "plugin.js",
  "icon": "icon.png",
  "permissions": [
    "library:read",
    "allow:downloads"
  ]
}
